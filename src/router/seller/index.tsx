import ProductCreatePage from "@/pages/Products/Create";
import { Route } from "react-router-dom";

const SellerRouters = [<Route path="product/create" element={<ProductCreatePage />} />];

export default SellerRouters;
